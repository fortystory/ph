[package]
name = "service"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
sqlx.workspace = true
chrono.workspace = true
uuid.workspace = true

core = { path = "../core" }
infra = { path = "../infra" }
